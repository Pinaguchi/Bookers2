<h1>Books</h1>
<% @books.each do |book| %>
<div>
  <p>投稿ユーザー画像：<%= image_tag book.user.get_profile_image("100x100") %></p>
  <p>ショップ名：<%= book.title %></p>
  <p>説明：<%= book.body %></p>
</div>
<% end %>